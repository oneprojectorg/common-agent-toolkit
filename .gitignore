node_modules/
.DS_Store
*.log
.idea/
.vscode/

# Skill-audit local artifacts — eval results may contain excerpts from the
# private codebase under test. Re-run evals locally to regenerate.
skill-audit/results/
.commitmsg
.prbody.md
.audit/

# Vitest eval artifacts — reports embed prompts and answers, which may quote the
# private codebase under test. Re-run `pnpm evals` locally to regenerate.
.vitest-evals/
