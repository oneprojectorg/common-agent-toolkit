node_modules/
.DS_Store
*.log
.idea/
.vscode/

# Eval artifacts — regenerated locally (`pnpm evals` / skill-audit). Reports
# may contain excerpts from the private codebase under test, so they are not
# committed.
skill-audit/results/
.vitest-evals/

# Local eval secrets (Hugging Face endpoint token).
.env
.env.local
.commitmsg
.prbody.md
.audit/
